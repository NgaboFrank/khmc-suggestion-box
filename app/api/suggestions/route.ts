import {NextResponse} from 'next/server';
import {createClient} from '@supabase/supabase-js';
export async function POST(req:Request){try{const body=await req.json();if(!body.type||!body.department||!body.message)return NextResponse.json({error:'Missing fields'},{status:400});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return NextResponse.json({error:'Supabase is not configured'},{status:500});const supabase=createClient(url,key);const {error}=await supabase.from('suggestions').insert({type:body.type,department:body.department,message:body.message,anonymous:body.anonymous==='true',name:body.anonymous==='true'?null:(body.name||null),phone:body.anonymous==='true'?null:(body.phone||null),status:'New'});if(error)throw error;return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Server error'},{status:500})}}