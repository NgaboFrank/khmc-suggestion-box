'use client';
import { FormEvent, useState } from 'react';
import { useSearchParams } from 'next/navigation';

export default function ResetPassword(){const params=useSearchParams();const token=params.get('token')||'';const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [done,setDone]=useState(false);const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');if(password.length<8)return setError('Password must be at least 8 characters');if(password!==confirm)return setError('Passwords do not match');setBusy(true);try{const r=await fetch('/api/admin/password-reset/confirm',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to reset password');setDone(true)}catch(e){setError(e instanceof Error?e.message:'Unable to reset password')}finally{setBusy(false)}}return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:24,background:'#f4f7fb',fontFamily:'Arial,sans-serif'}}><section style={{width:'min(440px,100%)',background:'#fff',borderRadius:22,padding:38,boxShadow:'0 20px 60px rgba(30,45,65,.12)'}}><img src="/khmc-logo.png" alt="KivuHills Medical Center" style={{width:170,maxHeight:110,objectFit:'contain',display:'block',margin:'0 auto 28px'}}/>{done?<><h1 style={{fontSize:28,color:'#172338'}}>Password changed</h1><p style={{fontSize:14,color:'#66758a'}}>Your admin password has been updated successfully.</p><a href="/admin" style={{display:'block',marginTop:22,color:'#2864e8',fontWeight:700}}>Go to login →</a></>:<><h1 style={{fontSize:28,color:'#172338',margin:'0 0 8px'}}>Create new password</h1><p style={{fontSize:14,color:'#66758a',margin:'0 0 24px'}}>Choose a new password for your admin account.</p><form onSubmit={submit} style={{display:'grid',gap:16}}><label style={{fontSize:13,fontWeight:700,color:'#172338'}}>New password<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} style={{display:'block',width:'100%',boxSizing:'border-box',marginTop:8,padding:'13px 14px',border:'1px solid #ccd7e5',borderRadius:9,fontSize:14}}/></label><label style={{fontSize:13,fontWeight:700,color:'#172338'}}>Confirm password<input type="password" minLength={8} required value={confirm} onChange={e=>setConfirm(e.target.value)} style={{display:'block',width:'100%',boxSizing:'border-box',marginTop:8,padding:'13px 14px',border:'1px solid #ccd7e5',borderRadius:9,fontSize:14}}/></label>{error&&<div style={{color:'#b14b45',fontSize:12}}>{error}</div>}<button disabled={busy||!token} style={{padding:14,border:0,borderRadius:9,background:'#2864e8',color:'#fff',fontWeight:700}}>{busy?'Updating…':'Update password'}</button></form></>}</section></main>}
