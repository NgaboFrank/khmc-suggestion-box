'use client';
import {useEffect,useState} from 'react';
export default function Admin(){const[items,setItems]=useState<any[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{fetch('/api/admin/suggestions').then(r=>r.json()).then(d=>setItems(d.suggestions||[])).finally(()=>setLoading(false))},[]);return <main className="admin"><div className="adminHead"><div><div className="brand">KHMC</div><h1>Suggestion Box</h1><p>Feedback received from patients.</p></div><a href="/">Patient Form</a></div><section className="table">{loading?<p>Loading...</p>:items.length===0?<p>No suggestions yet.</p>:items.map(x=><article className="item" key={x.id}><div><b>{x.type}</b><span>{x.department}</span><small>{new Date(x.created_at).toLocaleString()}</small></div><p>{x.message}</p><small>{x.anonymous?'Anonymous':`${x.name||''} ${x.phone||''}`}</small></article>)}</section></main>}