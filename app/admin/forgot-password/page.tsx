'use client';
import { FormEvent, useState } from 'react';

export default function ForgotPassword() {
  const [email,setEmail]=useState(''); const [sent,setSent]=useState(false); const [error,setError]=useState(''); const [busy,setBusy]=useState(false);
  async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/admin/password-reset/request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to send reset email');setSent(true)}catch(e){setError(e instanceof Error?e.message:'Unable to send reset email')}finally{setBusy(false)}}
  return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:24,background:'#f4f7fb',fontFamily:'Arial,sans-serif'}}><section style={{width:'min(440px,100%)',background:'#fff',borderRadius:22,padding:38,boxShadow:'0 20px 60px rgba(30,45,65,.12)'}}><img src="/khmc-logo.png" alt="KivuHills Medical Center" style={{width:170,maxHeight:110,objectFit:'contain',display:'block',margin:'0 auto 28px'}}/><h1 style={{fontSize:28,color:'#172338',margin:'0 0 8px'}}>Reset password</h1><p style={{fontSize:14,color:'#66758a',lineHeight:1.5,margin:'0 0 24px'}}>Enter your real admin email and we’ll send you a secure password reset link.</p>{sent?<><div style={{padding:15,borderRadius:10,background:'#eef8f2',color:'#176b43',fontSize:14,lineHeight:1.5}}>If that email belongs to an active admin account, a reset link has been sent. Check your inbox.</div><a href="/admin" style={{display:'block',marginTop:22,color:'#2864e8',fontWeight:700,fontSize:13}}>← Back to login</a></>:<form onSubmit={submit} style={{display:'grid',gap:16}}><label style={{fontSize:13,fontWeight:700,color:'#172338'}}>Admin email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="admin@example.com" style={{display:'block',width:'100%',boxSizing:'border-box',marginTop:8,padding:'13px 14px',border:'1px solid #ccd7e5',borderRadius:9,fontSize:14}}/></label>{error&&<div style={{color:'#b14b45',fontSize:12}}>{error}</div>}<button disabled={busy} style={{padding:14,border:0,borderRadius:9,background:'#2864e8',color:'#fff',fontWeight:700,cursor:'pointer'}}>{busy?'Sending…':'Send reset link'}</button><a href="/admin" style={{color:'#2864e8',fontSize:12,textAlign:'center'}}>← Back to login</a></form>}</section></main>;
}
